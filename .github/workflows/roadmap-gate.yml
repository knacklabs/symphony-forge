name: roadmap-gate

# Vendored to clients; no-ops in the harness (no VENDORED_FROM) and in
# clients that have not authored a roadmap with epics yet.
on:
  pull_request:
    # This gate reads the PR BODY, and its failure message tells you to edit
    # that body. Without `edited` the edit re-runs nothing and a manual re-run
    # replays the stale event payload, so the instruction cannot be followed.
    types: [opened, synchronize, reopened, edited]
  push:

permissions:
  contents: read

jobs:
  pr-contract:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Arm client roadmap gates
        id: arm
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          roadmap = Path("plans/roadmap.json")
          has_epics = False
          if roadmap.exists():
              data = json.loads(roadmap.read_text())
              has_epics = (
                  isinstance(data, dict)
                  and isinstance(data.get("epics"), list)
                  and len(data["epics"]) >= 1
              )
          armed = Path("constitution/VENDORED_FROM").is_file() and has_epics
          with open(os.environ["GITHUB_OUTPUT"], "a") as output:
              print(f"armed={str(armed).lower()}", file=output)
          PY
      - name: Require every completed work record to be declared
        if: steps.arm.outputs.armed == 'true'
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
          PR_BODY: ${{ github.event.pull_request.body }}
        # Diff against the MERGE-BASE, not pull_request.base.sha (which goes
        # stale as main advances and sweeps in main's own work records).
        run: |
          git fetch --no-tags origin "$BASE_REF"
          BASE_SHA="$(git merge-base FETCH_HEAD "$HEAD_SHA")"
          python3 factory/scripts/check_pr_ticket.py \
            --base "$BASE_SHA" \
            --head-branch "$HEAD_BRANCH" \
            --pr-body "$PR_BODY"
      - name: Require a shipped task to carry its recorded proof
        if: steps.arm.outputs.armed == 'true'
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        # The proof gate otherwise lives only inside `stage done`/`pr-ready`, so
        # a task merged through a direct or story-level PR skipped verify, tests
        # and the three-lens review silently. Checking the PR itself is what
        # makes the per-task flow un-bypassable (0049).
        run: |
          git fetch --no-tags origin "$BASE_REF"
          BASE_SHA="$(git merge-base FETCH_HEAD "$HEAD_SHA")"
          python3 factory/scripts/check_task_proof.py --base "$BASE_SHA"

  coverage:
    if: github.event_name == 'push' && github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Arm client roadmap gates
        id: arm
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          roadmap = Path("plans/roadmap.json")
          has_epics = False
          if roadmap.exists():
              data = json.loads(roadmap.read_text())
              has_epics = (
                  isinstance(data, dict)
                  and isinstance(data.get("epics"), list)
                  and len(data["epics"]) >= 1
              )
          armed = Path("constitution/VENDORED_FROM").is_file() and has_epics
          with open(os.environ["GITHUB_OUTPUT"], "a") as output:
              print(f"armed={str(armed).lower()}", file=output)
          PY
      - name: Audit project records on the default branch
        if: steps.arm.outputs.armed == 'true'
        run: python3 factory/scripts/forge.py project audit
