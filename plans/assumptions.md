# Implementation Assumptions Ledger

One row per assumption made during implementation (`forge plan assume`).
The orchestrator reviews open rows and guides:
`./forge assumptions resolve <id> --status confirmed|fix-needed|promoted --notes "..."`.
`pr_ready.py` refuses while the task has rows at `open` or `fix-needed`.

| id | date | issue | assumption | status | guidance |
|----|------|-------|------------|--------|----------|
| A-0001 | 2026-07-27 | FORGE-DELEG-1 | stage done exempts only .factory/ and plans/, NOT pr_ready's EVIDENCE_PATHS as the plan said — that list exempts all of factory/ and docs/, which in this repo is the product, so reusing it would make the write_scope check vacuous exactly where it is dogfooded | confirmed | Confirmed: stage measurement must exempt only workflow state, not factory/ or docs/, because those are product paths in the harness repository. The implementation and tests enforce the narrower boundary. |
| A-0002 | 2026-07-27 | FORGE-DELEG-1 | required_tests entries are test NAMES by existing convention (see any recorded decomposition), not file paths as the plan assumed — resolution is a fixed-string search across tracked and untracked files, excluding .factory/ and plans/ so the declaration cannot match itself | confirmed | Confirmed for the original decomposition contract: required test names were resolved defensively without matching workflow state. The later approved DELEG-8 amendment replaces legacy strings with explicit id, path, and command proof objects. |
| A-0003 | 2026-07-27 | FORGE-DELEG-1 | stage start also records dirty_at_start and stage done subtracts it: a file already dirty when the stage began is not that stage's work, and blaming it for one would refuse honest runs over unrelated edits | confirmed | Confirmed: dirty-at-start content is baseline context, not stage contribution. Digest and Git identity tracking preserve later edits while refusing unchanged inherited dirt. |
| A-0004 | 2026-08-04 | PH-1 | PH-1.3's heading check is a shallow pre-filter: it matches heading-shaped lines anywhere in the spec body, so a spec whose only required sections sit inside a fenced example or raw HTML passes it and is caught by the mandatory spec grill instead. This narrows the task's acceptance criterion, which reads as though the gate refuses every structurally incomplete spec. Chosen because three attempts at real structure detection each closed one over-count and opened a new FALSE REFUSAL of a complete spec, and correct detection needs a Markdown parser that factory/scripts is stdlib-only by design to exclude (D-0002). | promoted | The shallow pre-filter this assumption hedged is gone: example_ranges now excludes fenced blocks, HTML comments, verbatim raw blocks and container blocks in one pass, so the story's 'complete or refused' criterion means what it says. What survives is the contract question underneath - the gate parses a documented CommonMark subset because factory/scripts is stdlib-only, and ambiguity resolves toward masking less. That is durable and belongs in decision 0020, not in a per-task assumption. -> 0020-markdown-structure-subset.md |
| A-0005 | 2026-08-05 | PH-4 | Project name means the repository root directory name, matching the board's existing root identity because no separate authored project-name field is in scope. | confirmed | Resolved by correction, not by confirmation: the original assumption (project name = repository directory name) was WRONG — forge init --name authors it into run.json. Implementation went further after story review showed run.json is lifecycle state a data-only tree never has: project_identity now reads three sources, most authoritative first — the authored name, then the BRIEF's H1 (committed; sign-off refuses without it), then the resolved directory. Documented in the function's docstring and pinned by test_project_name_survives_the_run_state_lifecycle (init -> intake -> shipped shape) and test_project_name_falls_back_to_the_brief_when_run_state_is_absent. |
| A-0006 | 2026-08-06 | FORGE-APPROVE-1 | The ExitPlanMode hook records approver as human-via-exit-plan-mode because Claude Code's documented PostToolUse payload contains the approved plan but no human identity. | promoted | Superseded by the pivot: the ExitPlanMode PostToolUse hook is infeasible (no plan-mode hook fires, #21282), so approver-identity-from-payload never applies. Decision 0029's correction replaces it with CLI 'plan approve --by <name>', which carries explicit human identity. -> 0029-plan-approval-in-plan-mode.md |
| A-0007 | 2026-08-08 | FORGE-BOARD-4 | Project backfill marks an evidence-less done card with backfill_evidence_missing=true and reconstructs only fields explicitly represented in its archived plan or history decomposition. | confirmed | Confirmed as the intended honest behaviour (project-record mark-don't-fabricate): backfill reconstructs a done card only from fields its archived plan/history decomposition states verbatim, and marks an evidence-less card backfill_evidence_missing rather than inventing content. Verified in the dogfood (harness-v2-wedge marked; the 10 recovered links all real). |
| A-0008 | 2026-08-09 | FORGE-BOARD-7 | forge mode abandon requires --reason for the abandonment, records the original opening reason separately, and preserves the harness-marker integrity check while bypassing Lite commit, manifest, and review gates. | confirmed | Verified in autoreview (security lens) against cmd_mode_abandon: refuses without an open window, requires a non-empty --reason, records opened_reason separately from the abandon reason, preserves _require_harness_marker, and bypasses only the mode-done completion gates. Implementation matches the assumption. |
| A-0009 | 2026-08-12 | FORGE-WIN-2 | Windows remediation uses the winget package Python.Python.3.12 because it is a maintained release satisfying the required Python 3.10 minimum. | confirmed | Correct call: Python.Python.3.12 is maintained, satisfies the >=3.10 floor, and avoids bleeding-edge 3.13+ compatibility risk with the harness's stdlib usage. Revisit only when 3.12 leaves support. |
| A-0010 | 2026-08-12 | FORGE-WIN-2 | Windows python winget pin corrected to Python.Python.3.14: 3.12 is security-only per the Python lifecycle; 3.14 is the current bugfix line with winget manifests. Supersedes A-0009's 3.12 rationale (orchestrator's earlier confirmation was factually stale). | confirmed | Verified against the Python release lifecycle and winget-pkgs manifests by the worker (S-0001-36a8 evidence); 3.14 is the pin. |
| A-0011 | 2026-08-15 | FORGE-CFS-1 | A story directory created by intake is the marker for story-scoped writes; without it, an active legacy story continues using live singletons. | confirmed | Confirmed: intake creates .factory/stories/<KEY>/ as the scoped-layout marker (intake.py:118); story_uses_scoped_layout keys on its existence; a legacy story without it keeps using live singletons. Operator-confirmed design (decision 0046, task-1 grill), proven by test_intake_on_legacy_fixture and the merge-simulation. |
