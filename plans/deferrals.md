# Deferral Ledger

Deliberately-removed scope with explicit revisit triggers (`forge defer add`).
When a trigger fires, the item goes back on the roadmap and its row is
resolved: `./forge defer resolve <id> --notes "<what happened>"`.

| id | added | item | why deferred | trigger to revisit | status |
|----|-------|------|--------------|--------------------|--------|
| D-0001 | 2026-07-28 | Container-backed containment for untrusted delegation and proof commands | True cross-platform hostile-process containment requires a pinned container image/runtime; process-table sampling cannot provide that boundary on macOS and Linux. | Forge permits untrusted decomposition commands or third-party worker code to execute with write access | open |
| D-0002 | 2026-08-04 | Abort or preserve-under-conflict when a client-added skill name collides with a skill the harness has since started shipping | Autoreview P1 on FORGE-UPG-1.1. Verified pre-existing and symmetric: the non-legacy preserve branch (upgrade.py:160) applies the identical rule, so a colliding client skill is already replaced on every current repo today. Changing it is a harness-wide decision about skill ownership and mixed-ownership semantics, not part of retiring the .agents/ tree, and doing it inside a migration story would change upgrade behavior for every repo under cover of a rename. | A real client repo loses a skill to a name collision, or the harness starts shipping a skill whose name is already common in client repos | open |
| D-0003 | 2026-08-04 | Audit every harness command that writes into a client repo (init, adopt, upgrade) against the path-boundary invariant, with tests per site | The recurring repository-escape class was confined to forge upgrade's new legacy-migration path and every site found is fixed and tested. init and adopt copy into a target the same way but were not touched by this story, so auditing them is a separate self-contained refactor rather than a fourth patch inside a migration story. | A fourth repository-escape finding lands in any client-writing command, or forge init/adopt gains new copy or delete logic | open |
| D-0004 | 2026-08-04 | Heading-shaped text inside a raw HTML block (<pre>, <div>) still counts as document structure in spec confirm and sign-off | Closing it means parsing Markdown properly, and factory/scripts is stdlib-only by design (factory_lib.py states it). Adding a Markdown dependency is a project-constraint change, not a task fix. The threat model is also weak: the gate serves the spec's own author, so bypassing it means deliberately faking structure to defeat a check that only helps you. Two autoreview cycles reached on this task; the governor says reclassify rather than patch a third time. | A spec or brief is found in a real project whose required sections exist only inside raw HTML, or the stdlib-only constraint is lifted for factory/scripts -> Closed by the branch review, not by the trigger: four autoreview rounds named concrete bypasses (fenced examples, HTML comments, indented top-level fences, list-nested fences, verbatim raw blocks, container blocks) and factory_lib.example_ranges now excludes all of them in one pass, with a regression test per case. What remains is not raw HTML but the rest of CommonMark - setext headings, link reference definitions, block quotes - and the stdlib-only constraint that makes a real parser a decision rather than a patch. That is a new, narrower question and belongs in its own record. | done |
| D-0005 | 2026-08-05 | Board page resolves from the --repo target, so a data-only directory cannot be served | board.py:535 reads index.html from the --repo target rather than from the running code, so ./forge board --repo <data-only-tree> cannot serve. The fix (resolve the page beside board.py) is right and behavior-neutral for ordinary repos, but PH-4's approved plan scopes Task 4 to example/tests/docs and declares CLI/ops unchanged by design — expanding scope mid-story to chase a convenience no roadmap criterion requires is the drift the plan gate exists to prevent. | Anyone needs to serve a data-only example or fixture directory with forge board, or board.py is next opened for other work | open |
| D-0006 | 2026-08-06 | The project record is incomplete, in this repo and in every client repo that adopts mid-flight | Opus audit, 2026-08-06: 16% of substantive commits (22 touching product code) and 30 of 36 merged PRs have no durable in-repo trace — a grep for PR numbers across plans/, .factory/ and docs/ returns zero hits. quickfix files[] is empty in 29 of 29 real records, so the bounded escape hatch neither binds nor reports. FORGE-INIT-1 and harness-v2-wedge shipped with no outcome.json. roadmap.json has lost items to merges twice. .factory/events.jsonl holds 314 committed events across 29 types with no reader. Client repos inherit all of it AND start with history predating the record, because forge adopt exists for repos with existing work. Design stance agreed with the user: do NOT backfill plausible-but-false records — write a 'record begins here' marker at init/adopt (date, commit, count of preceding commits) so the board reports honest incompleteness instead of implying the project began at adoption. Sequence by leverage: forge history over events.jsonl first (smallest, makes the rest visible), then capture completeness (PR links, outcomes for non-story work, live quickfix files[]), then board truth (cross-worktree view, task depth on cards). | PH-4 is merged, or any client repo asks what was built before adoption, or a second shipped story is found with no outcome | open |
| D-0007 | 2026-08-07 | Harmonize the product/machinery classification across pre_tool_use.py, pr_ready.py (EVIDENCE_PATHS), check_refactor_delta.py (EXCLUDE_PREFIXES) and stages.py (WORKFLOW_PATHS) onto one shared repo-kind-aware classifier (is_harness_source_repo seam introduced in FORGE-LOCK-1) | The split is duplicated in 4 differently-tuned copies; FORGE-LOCK-1 made only the lock repo-kind-aware to stay bounded (0005 spirit — no 5th tuned copy). Fifth-site progress (FORGE-ROLE-1): the new session locked-path boundary now lives in repo_kind.py and pre_tool_use consumes it; the four named legacy classifiers still need harmonizing. | any of the 4 classifiers needs a path-set change again, or stages.py delta under-counts harness-repo machinery churn | open |
| D-0008 | 2026-08-07 | Quickfix 5-file budget under-counts directory/glob destinations: 'cp x factory/scripts/' and 'rm -r dir' record the container as one claimed path while creating/deleting many files. Pre-existing bash_write_paths/claim_files granularity (affects client src/ identically); newly exposed for harness factory/ by FORGE-LOCK-1. Resolve directory destinations to basenames or refuse opaque recursive/glob ops under a quickfix. | Out of scope for the repo-kind lock; the quickfix repo-kind PIN already prevents the marker-escape that FORGE-LOCK-1 is about. This is a general budget-accuracy limitation. | a quickfix budget is bypassed in practice via a directory/glob write, or the quickfix budget is next revised -> Addressed in FORGE-LOCK-1: the hook now REFUSES opaque product writes under a quickfix (recursive rm, glob operands, copy/move into a machinery directory) via has_opaque_product_write, so the 5-file budget stays honest by rejecting un-boundable ops rather than under-counting them. Enumerate exact paths or plan the change. | done |
| D-0009 | 2026-08-07 | Guard cmd_mode_done's shutil.rmtree('.factory/reviews') to remove only the artifacts the lite window archived. | An unconditional rmtree on lite close could delete a concurrent story's review artifacts if a lite window is opened+closed during that story's review phase (edge case; lite is post-ship-oriented). | lite mode is used mid-story while an approved plan is active, OR a report of lost .factory/reviews after mode done | open |
| D-0010 | 2026-08-07 | Board page does not render a story's recorded outcome: the served frontend has zero 'outcome' references, so a shipped story's drawer (spec: 'shipped stories lead with the outcome') and the Ship log show no outcome paragraph even though /api/story now carries evidence.outcome (added in PR #51) and the roadmap item has it. | The board-page frontend is partially wired (see D-0005, the deferred board-page resolver); rendering outcome is frontend work beyond the PR #51 backend crash fix, and no roadmap criterion required it. | board.py/frontend is next opened for work, or a user asks why the board doesn't show shipped outcomes | open |
| D-0011 | 2026-08-07 | Enable board-invariant.yml on push:main once the harness's legacy done stories are backfilled with pr-links or predates markers. | The 10 pre-automation done stories carry no pr-linked event, so check_board_complete fails on them; enabling the invariant live before the backfill would red main. It ships dispatch-only. | the legacy done stories are backfilled (project-record/backfill story), or the board is otherwise clean -> Resolved by FORGE-BOARD-4.3: legacy done stories were backfilled with verified PR links or an honest predates marker, check_board_complete passes, and board-invariant now runs on push to main. | done |
| D-0012 | 2026-08-07 | Make Gate A (check_pr_ticket) also accept a story that is ADDED and completed in the same PR (base-absent -> head-done + history), for bundled/bootstrapping PRs. | The intended model adds stories to the roadmap before their impl PR (base-present), so Gate A requires a base->done flip; a PR that both adds and completes a story (like this bootstrapping PR) is not recognized and reds its own self-test. | a harness or client PR bundles roadmap-add + implementation, or Gate A reds a legitimately-complete PR -> Fixed in PR #58: check_pr_ticket accepts base-absent story completion (bundled PRs). | done |
| D-0013 | 2026-08-08 | pr_ready archives .factory/grills/tasks/ into story history | Task-grill artifacts should be archived per story like grills/plan.json (0025 evidence lifetime); mis-scoped out of FORGE-BOARD-2.2 (pr_ready.py not in write_scope). | next traceable-board story touching pr_ready, or before the epic ships -> Implemented: pr_ready archives .factory/grills/tasks/ into story history then removes it (mirrors plan.json) | done |
| D-0014 | 2026-08-08 | forge project backfill matcher should also recover a PR whose BODY names the story key | The title/branch matcher misses early PRs with non-conventional titles (e.g. FORGE-DELEG-1 shipped in #24, whose body says 'Archives the completed FORGE-DELEG-1' but whose title/branch do not match); those need a manual forge pr-link today. Reading the PR body (committed evidence) would recover them deterministically. | next upgrade-skill work (FORGE-BOARD-5), or a legacy repo with non-conventional PR titles -> Implemented: forge project backfill matches a PR by its body naming the story key (word-boundary) | done |
| D-0015 | 2026-08-10 | Write-time PostToolUse hook for context-ledger freshness | Write-time scanning changes no gate outcome because pending_context already counts unscanned files; commit-time freshness is enforced by the commit belt and unchanged CI check. | stale-ledger CI failures recur from out-of-session commits of agent-written context files, or a second consumer of write-time ledger freshness appears | open |
| D-0016 | 2026-08-10 | Make factory-scaffold.yml and harness-health.yml push legs follow the repository default branch instead of hardcoding main | This is a pre-existing default-branch defect in two established vendored workflows; retrofitting their triggers is separate from the new roadmap gate and outside FORGE-ALIGN-3. | a client whose default branch is not main reports a never-firing push workflow, or the myclaw/minegate pilot lands on a non-main default branch | open |
| D-0017 | 2026-08-10 | link-pr auto-commit strands PR checks in action_required (bot-triggered runs held by GitHub) | The link-pr workflow's commit re-triggers all PR workflows as github-actions[bot], which GitHub holds for approval; the rollup then shows no checks until a human pushes again. Cost one manual empty-commit re-trigger on PR #72. | a second PR strands in action_required after the link commit, or link-pr is next opened for work -> Fixed: the link commit carries [skip ci], so the bot-attributed synchronize wave is never created and nothing strands; pinned by test_pr_link_commit_skips_ci. Residual cosmetic: gh pr checks on the link-commit head shows the prior sha's runs one commit back. | done |
| D-0018 | 2026-08-12 | forge.cmd native fallback omits python3.exe probe (py -3/python only) — a Git-Bash-less box with python3-only install is wrongly rejected | Review of merged 95062be (product target): gap is in the unsupported no-Git-Bash lane; supported path unaffected | FORGE-WIN-2 planning (its remediation owns the no-Git-Bash path) -> Folded into approved FORGE-WIN-2 plan (task WIN-2.2): python3.exe probe as third forge.cmd fallback with the two text-assertion updates | done |
| D-0019 | 2026-08-12 | doctor hook-health certifies via sh -c, but the real Windows hook executor without Git Bash (PowerShell/cmd) parses the registered command differently — doctor can falsely certify on unsupported executors | Same review: certification fidelity vs actual executor; supported Git Bash path certified honestly | FORGE-WIN-3 planning (native Windows execution model) or first client report of doctor-green-but-hooks-dead | open |
| D-0020 | 2026-08-12 | test_gates.py:2338 lock-contention assertion runs fake msvcrt on the nt branch — deterministic failure whenever the windows CI selector widens to the full suite | Tests-target review of merged 95062be: latent, unexercised by the current 9-test windows selector; POSIX behavior correct | widening the windows-hook-gates selector or FORGE-WIN-3 planning | open |
| D-0021 | 2026-08-12 | Harness-orchestrated single-UAC elevation batch (Start-Process -Verb RunAs running winget) — cannot be built safely blind: an elevated process inheriting a user-controllable PATH is an irreducible privilege-escalation hole, refuted across 4 autoreview rounds (elevation-trust class, decision 0005 escalation). Build and validate it against a real Windows environment where each fix confirms in minutes. | Recurring elevation-trust findings (WIN-2.1 review rounds 3/4/6/8); blind iteration cannot settle Windows elevation-boundary semantics | Windows VM/real-Windows validation loop exists, or FORGE-WIN-3 (native Windows execution) planning | open |
| D-0022 | 2026-08-12 | doctor's read-only executable probes (_python_check and the git/node/direnv version checks) exec PATH-resolved binaries; run from an elevated admin shell where a same-user attacker controls a user-writable PATH entry, this is a privesc vector — but so is running ANY dev tool as admin, and refusing all probes when elevated would gut doctor. The STATE-CHANGING remediation path already short-circuits when elevated (WIN-2.4); this residual is the read-only probes only. | Elevation-trust class recurred ~7x across WIN-2 review; the install/execute-to-modify risk is closed, the read-only-probe residual needs a dedicated security-design pass on real Windows, not blind iteration | real-Windows security validation, or FORGE-WIN-3 (native execution) planning, or a client report of doctor-under-admin concern | open |
| D-0023 | 2026-08-13 | Windows bootstrap/binary-trust residuals needing real-Windows security validation: (a) forge.cmd bootstrap executes %LOCALAPPDATA%\Microsoft\WindowsApps\winget.exe without authenticating the binary — same-security-context when unelevated (no privesc), elevated is guarded; (b) the bootstrap re-exec cmd /d /c passes %* through a nested cmd, reparsing literal-percent args — bootstrap-path-only, negligible for realistic forge args, but the guard-scope-vs-reparse tension needs real-Windows testing to resolve without reintroducing the 2.3 guard leak. | Story-review round 4 residuals; all state-changing security paths closed, these are hostile-env/batch-arcana edges best settled on real Windows alongside D-0022 | real-Windows security validation, or FORGE-WIN-3 (native execution) planning | open |
| D-0024 | 2026-08-12 | Allowlist the companion 'result' verb in the read-only guard lane so a completed run's output is fetchable without log-tailing | FORGE-ROLE-2 is bounded to the task-verb --prompt-file admission; result is a different verb with different output semantics | next time an orchestrating session must tail a job log to read a completed companion result | open |
| D-0025 | 2026-08-13 | Per-task sandbox-exception plumbing in delegate, recording mode and rationale in the protected delegation record | the installed codex plugin accepts only a write toggle and hardcodes workspace-write, so a delegate-side escalation flag would be dead machinery until an upstream release | the codex plugin gains a sandbox override option | open |
| D-0026 | 2026-08-13 | Inline workflow Python in .github/workflows/{roadmap-gate,pr-link}.yml default-decodes roadmap JSON and GITHUB_OUTPUT appends (sol-xhigh validation, FORGE-WIN-ENC plan gate) | FORGE-WIN-ENC's sweep + hygiene gate are scoped to factory/scripts/**; the workflow-embedded snippets run on GH's UTF-8-friendly runners where the locale default currently holds | first mojibake or UnicodeDecodeError in a gate-workflow log, or any new inline workflow Python that parses repo files | open |
| D-0027 | 2026-08-18 | stages.json (stage start) routes story-scoped under .factory/stories/<KEY>/ | Out of scope for story-scoped-evidence: forge_cli/stages.py is not in this task's write_scope; task 1 covers the decomposition/grill/test/review recorders + verify only. | run-pointer-untracked or ship-in-place task starts — confirm stages.json is story-scoped there or add a dedicated task | open |
| D-0028 | 2026-08-18 | outcome.json routes story-scoped under .factory/stories/<KEY>/ | Out of scope for story-scoped-evidence: outcome is written at ship; belongs with pr_ready changes. | ship-in-place task starts — confirm outcome.json is story-scoped in the no-move ship path | open |
| D-0029 | 2026-08-18 | briefs route story-scoped under .factory/stories/<KEY>/ | Out of scope for story-scoped-evidence: brief composition is delegate-side; not among the four in-scope recorders. | migration-and-vendor task starts — confirm briefs migrate to the story layout | open |
