# Deferral Ledger

Deliberately-removed scope with explicit revisit triggers (`forge defer add`).
When a trigger fires, the item goes back on the roadmap and its row is
resolved: `./forge defer resolve <id> --notes "<what happened>"`.

| id | added | item | why deferred | trigger to revisit | status |
|----|-------|------|--------------|--------------------|--------|
| D-0001 | 2026-07-28 | Container-backed containment for untrusted delegation and proof commands | True cross-platform hostile-process containment requires a pinned container image/runtime; process-table sampling cannot provide that boundary on macOS and Linux. | Forge permits untrusted decomposition commands or third-party worker code to execute with write access | open |
| D-0002 | 2026-08-04 | Abort or preserve-under-conflict when a client-added skill name collides with a skill the harness has since started shipping | Autoreview P1 on FORGE-UPG-1.1. Verified pre-existing and symmetric: the non-legacy preserve branch (upgrade.py:160) applies the identical rule, so a colliding client skill is already replaced on every current repo today. Changing it is a harness-wide decision about skill ownership and mixed-ownership semantics, not part of retiring the .agents/ tree, and doing it inside a migration story would change upgrade behavior for every repo under cover of a rename. | A real client repo loses a skill to a name collision, or the harness starts shipping a skill whose name is already common in client repos | open |
| D-0003 | 2026-08-04 | Audit every harness command that writes into a client repo (init, adopt, upgrade) against the path-boundary invariant, with tests per site | The recurring repository-escape class was confined to forge upgrade's new legacy-migration path and every site found is fixed and tested. init and adopt copy into a target the same way but were not touched by this story, so auditing them is a separate self-contained refactor rather than a fourth patch inside a migration story. | A fourth repository-escape finding lands in any client-writing command, or forge init/adopt gains new copy or delete logic | open |
| D-0004 | 2026-08-04 | Heading-shaped text inside a raw HTML block (<pre>, <div>) still counts as document structure in spec confirm and sign-off | Closing it means parsing Markdown properly, and factory/scripts is stdlib-only by design (factory_lib.py states it). Adding a Markdown dependency is a project-constraint change, not a task fix. The threat model is also weak: the gate serves the spec's own author, so bypassing it means deliberately faking structure to defeat a check that only helps you. Two autoreview cycles reached on this task; the governor says reclassify rather than patch a third time. | A spec or brief is found in a real project whose required sections exist only inside raw HTML, or the stdlib-only constraint is lifted for factory/scripts -> Closed by the branch review, not by the trigger: four autoreview rounds named concrete bypasses (fenced examples, HTML comments, indented top-level fences, list-nested fences, verbatim raw blocks, container blocks) and factory_lib.example_ranges now excludes all of them in one pass, with a regression test per case. What remains is not raw HTML but the rest of CommonMark - setext headings, link reference definitions, block quotes - and the stdlib-only constraint that makes a real parser a decision rather than a patch. That is a new, narrower question and belongs in its own record. | done |
| D-0005 | 2026-08-05 | Board page resolves from the --repo target, so a data-only directory cannot be served | board.py:535 reads index.html from the --repo target rather than from the running code, so ./forge board --repo <data-only-tree> cannot serve. The fix (resolve the page beside board.py) is right and behavior-neutral for ordinary repos, but PH-4's approved plan scopes Task 4 to example/tests/docs and declares CLI/ops unchanged by design — expanding scope mid-story to chase a convenience no roadmap criterion requires is the drift the plan gate exists to prevent. | Anyone needs to serve a data-only example or fixture directory with forge board, or board.py is next opened for other work | open |
| D-0006 | 2026-08-06 | The project record is incomplete, in this repo and in every client repo that adopts mid-flight | Opus audit, 2026-08-06: 16% of substantive commits (22 touching product code) and 30 of 36 merged PRs have no durable in-repo trace — a grep for PR numbers across plans/, .factory/ and docs/ returns zero hits. quickfix files[] is empty in 29 of 29 real records, so the bounded escape hatch neither binds nor reports. FORGE-INIT-1 and harness-v2-wedge shipped with no outcome.json. roadmap.json has lost items to merges twice. .factory/events.jsonl holds 314 committed events across 29 types with no reader. Client repos inherit all of it AND start with history predating the record, because forge adopt exists for repos with existing work. Design stance agreed with the user: do NOT backfill plausible-but-false records — write a 'record begins here' marker at init/adopt (date, commit, count of preceding commits) so the board reports honest incompleteness instead of implying the project began at adoption. Sequence by leverage: forge history over events.jsonl first (smallest, makes the rest visible), then capture completeness (PR links, outcomes for non-story work, live quickfix files[]), then board truth (cross-worktree view, task depth on cards). | PH-4 is merged, or any client repo asks what was built before adoption, or a second shipped story is found with no outcome | open |
| D-0007 | 2026-08-07 | Harmonize the product/machinery classification across pre_tool_use.py, pr_ready.py (EVIDENCE_PATHS), check_refactor_delta.py (EXCLUDE_PREFIXES) and stages.py (WORKFLOW_PATHS) onto one shared repo-kind-aware classifier (is_harness_source_repo seam introduced in FORGE-LOCK-1) | The split is duplicated in 4 differently-tuned copies; FORGE-LOCK-1 made only the lock repo-kind-aware to stay bounded (0005 spirit — no 5th tuned copy) | any of the 4 classifiers needs a path-set change again, or stages.py delta under-counts harness-repo machinery churn | open |
| D-0008 | 2026-08-07 | Quickfix 5-file budget under-counts directory/glob destinations: 'cp x factory/scripts/' and 'rm -r dir' record the container as one claimed path while creating/deleting many files. Pre-existing bash_write_paths/claim_files granularity (affects client src/ identically); newly exposed for harness factory/ by FORGE-LOCK-1. Resolve directory destinations to basenames or refuse opaque recursive/glob ops under a quickfix. | Out of scope for the repo-kind lock; the quickfix repo-kind PIN already prevents the marker-escape that FORGE-LOCK-1 is about. This is a general budget-accuracy limitation. | a quickfix budget is bypassed in practice via a directory/glob write, or the quickfix budget is next revised -> Addressed in FORGE-LOCK-1: the hook now REFUSES opaque product writes under a quickfix (recursive rm, glob operands, copy/move into a machinery directory) via has_opaque_product_write, so the 5-file budget stays honest by rejecting un-boundable ops rather than under-counting them. Enumerate exact paths or plan the change. | done |
