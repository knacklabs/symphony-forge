{
  "generated_by": "claude-code:plan-mode",
  "project": "",
  "story": "FORGE-CFS-1",
  "user_facing": false,
  "doc_roots": [
    "docs/specs/conflict-free-story-state.md",
    "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md"
  ],
  "epics": [
    {
      "id": "accountable-engineering",
      "title": "Accountable engineering loop",
      "objective": "Enforce decision 0032 end to end: JIT frontier contracts gated at stage start, evidence-backed grills, bounded reviewable diffs.",
      "source_refs": [
        "docs/specs/conflict-free-story-state.md"
      ]
    }
  ],
  "tasks": [
    {
      "id": "story-scoped-evidence",
      "title": "Evidence records under the story directory",
      "objective": "A single story_dir() path helper routes every recorder and gate to .factory/stories/<KEY>/ for new stories, with read fallback to the legacy live singletons and history/ so existing data stays readable.",
      "acceptance_criteria": [
        "the in-scope recorders (decomposition, grill, test, review) and verify write and read new-story artifacts under .factory/stories/<KEY>/ through one shared path helper",
        "legacy live and history layouts remain readable by those same consumers"
      ],
      "dependencies": [],
      "write_scope": [
        "factory/scripts/factory_lib.py",
        "factory/scripts/record_decomposition_from_json.py",
        "factory/scripts/record_grill_from_json.py",
        "factory/scripts/record_test_from_json.py",
        "factory/scripts/record_review_from_json.py",
        "factory/scripts/verify.py",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_new_story_artifacts_record_under_story_dir",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_legacy_layout_stays_readable_by_every_consumer",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "Single-source resolution is the whole game: story_dir(root, key) plus one evidence_path(root, key, name) accessor in factory_lib must be the ONLY place layout knowledge lives; a recorder or gate joining paths by hand forks the layout. Write-new/read-both: writers go story-scoped for stories intaken under the new layout, readers fall back legacy-first-miss. SCOPE BOUNDARY: this task covers the recorders in write_scope (decomposition, grill, test, review) and verify ONLY; plans.py and pr_ready.py read the plan grill / archive by hand but are the migration-and-vendor task's job - the required test must NOT exercise those paths (three prior signals hit exactly that). A budget overrun is the split signal, not a reason-raise.",
      "plan_contracts": [
        {
          "id": "CFS1-C1",
          "statement": "the in-scope recorders (decomposition, grill, test, review) and verify write and read new-story artifacts under .factory/stories/<KEY>/ through one shared path helper",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS1-C2",
          "statement": "legacy live and history layouts remain readable by those same consumers",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    },
    {
      "id": "run-pointer-untracked",
      "title": "The active-story pointer leaves version control",
      "objective": "Move the active-story pointer out of version control: run_state_path() resolves to a worktree-local untracked pointer under the git control dir, with tracked .factory/run.json still readable for legacy repos. New-layout stories write only the untracked pointer; phase derives from story-dir artifact presence. Single-source in factory_lib so the ~20 read consumers follow unchanged.",
      "acceptance_criteria": [
        "two worktrees intake different stories with zero tracked run.json writes",
        "phase derivation matches the old run.json semantics for legacy repos"
      ],
      "dependencies": [
        "story-scoped-evidence"
      ],
      "write_scope": [
        "factory/scripts/factory_lib.py",
        "factory/scripts/intake.py",
        "factory/scripts/update_run.py",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_intake_writes_untracked_pointer_zero_tracked_run_json",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_phase_derivation_matches_legacy_run_json_semantics",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_board_shows_task_grill_status",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_board_shows_done_story_pr_link",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "Single-source run_state_path is the whole game (same discipline as story_dir in task 1): the ~20 read consumers follow the helper and must NOT be edited individually. The untracked pointer MUST resolve via the git control dir (git rev-parse --absolute-git-dir), never a literal root/.git join - in a linked worktree .git is a FILE, so a naive join writes the pointer to the wrong place and merges can still see it. Read resolves untracked-first then legacy .factory/run.json; write goes untracked only for new-layout (marker present), legacy stays tracked. Phase derives from story-dir artifact presence, matching the old stored-phase semantics for legacy. SCOPE BOUNDARY: does not touch read consumers, pr_ready archival (ship-in-place), event files (per-event-files), or intake-marker migration for existing repos (migration-and-vendor); required tests must not exercise those.",
      "plan_contracts": [
        {
          "id": "CFS2-C1",
          "statement": "two worktrees intake different stories with zero tracked run.json writes",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS2-C2",
          "statement": "phase derivation matches the old run.json semantics for legacy repos",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    },
    {
      "id": "per-event-files",
      "title": "Events are one file each",
      "objective": "append_event writes each event as its own conflict-free file (retiring the events.jsonl union-merge driver) instead of appending to the shared ledger; load_events / forge history / the board read per-event files and the legacy events.jsonl together. Single-source in events.py so the ~20 callers and the board follow unchanged. Marker-neutral per decision 0046.",
      "acceptance_criteria": [
        "recording an event creates a new file and appends to no shared ledger",
        "forge history renders legacy and per-file events in one timeline"
      ],
      "dependencies": [
        "run-pointer-untracked"
      ],
      "write_scope": [
        "factory/scripts/forge_cli/events.py",
        ".gitattributes",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_append_event_writes_new_file_no_shared_ledger",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_history_merges_legacy_and_per_file_events",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "Single-source append_event/load_events is the whole game: the ~20 callers and board.py read through load_events and must NOT be edited individually. Per-event files are conflict-free by unique name; append_event must STOP appending to the shared events.jsonl (that retires the union driver) and the merge=union entry for events.jsonl must be removed from .gitattributes. load_events reads per-event files AND legacy events.jsonl, preserving the story/event/since/until filters and the per-line JSONDecodeError tolerance (a torn legacy line costs one event, never the timeline). append_event stays best-effort on write (OSError swallowed) but validate_payload is NOT swallowed. Marker-neutral per 0046: events touch no deferred consumer (plans.py/pr_ready), so retirement is unconditional and does not activate the story-scoped layout. SCOPE: does not touch the callers, board.py, plans.py, or pr_ready.",
      "plan_contracts": [
        {
          "id": "CFS3-C1",
          "statement": "recording an event creates a new file and appends to no shared ledger",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS3-C2",
          "statement": "forge history renders legacy and per-file events in one timeline",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    },
    {
      "id": "ship-in-place",
      "title": "pr_ready ships without moving files",
      "objective": "For a new-layout story (marker present), pr_ready marks it shipped INSIDE its story directory - no archive move, no rename strands - and outcome.json routes story-scoped; a legacy story (marker off) keeps the existing archive-to-history flow unchanged. Board and forge history read shipped story dirs. Roadmap done-flip unchanged. Marker-neutral per decision 0046.",
      "acceptance_criteria": [
        "pr_ready completes with zero file moves and evidence stays at its recorded paths",
        "board and forge history read shipped stories from the story directory"
      ],
      "dependencies": [
        "per-event-files"
      ],
      "write_scope": [
        "factory/scripts/pr_ready.py",
        "factory/scripts/forge_cli/outcome.py",
        "factory/scripts/forge_cli/board.py",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_pr_ready_ships_in_place_no_file_moves",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_board_and_history_read_shipped_story_dir",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_pr_ready_legacy_story_still_archives_to_history",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "The ship path MUST branch on the scoped-layout marker: new-layout (marker present) ships IN PLACE - the story dir already holds all evidence (tasks 1-3), so pr_ready records shipped state in the story dir (a shipped marker or derive_phase's shipped rung) and performs ZERO shutil.move; a legacy story (marker off) keeps the exact archive-to-history + plans/completed move it does today (0046: no production story runs new-layout yet, so the legacy path must stay bit-for-bit unchanged - the third required test guards it). outcome.json routes through evidence_path (D-0028). pr_ready STOPS its own grill archival (S-0002) but does NOT touch plans.py (S-0001 defer to migration-and-vendor). Idempotent rerun for new-layout keys on the in-place shipped marker, not the history archive. board.py is IN scope for the READ path only: it hard-codes story-evidence roots as .factory (active) or .factory/history/<key> (shipped) and must resolve .factory/stories/<KEY>-first with .factory and .factory/history fallback (task 1's story-first/legacy-fallback discipline). This is read-only - board never breaks on activation and reads legacy in production while the marker is off (0046). forge history already reads via load_events/evidence_path. SCOPE: pr_ready + outcome recorder + tests only.",
      "plan_contracts": [
        {
          "id": "CFS4-C1",
          "statement": "pr_ready completes with zero file moves and evidence stays at its recorded paths",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS4-C2",
          "statement": "board and forge history read shipped stories from the story directory",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    },
    {
      "id": "merge-survivable-hooks",
      "title": "Hooks survive merges and permit scoped resolution",
      "objective": "pre_tool_use and stop_continue survive a merge: wrap the factory_lib/forge_cli imports and state loads so an unparseable factory_lib or run.json engages a SELF-CONTAINED static deny-list (fail-closed on product writes, permits git-native recovery + read-only) instead of crashing. With unmerged index entries, pre_tool_use permits git-native resolution for exactly those paths while refusing content hand-writes. forge next auto-heals the roadmap after a merge.",
      "acceptance_criteria": [
        "the carve-out matrix passes: resolution allowed for exactly the unmerged set, refused outside it, deny-list fallback engages on broken imports",
        "forge next heals the roadmap after a merge without being asked"
      ],
      "dependencies": [
        "ship-in-place"
      ],
      "write_scope": [
        "factory/scripts/pre_tool_use.py",
        "factory/scripts/stop_continue.py",
        "factory/scripts/forge_cli/phase.py",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_hook_denylist_fallback_on_unparseable_state_or_import",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_hook_permits_git_native_resolution_on_unmerged_paths",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_hook_refuses_handwrite_and_merged_paths_during_merge",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_forge_next_auto_heals_roadmap_after_merge",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_jsonl_ledgers_merge_with_a_builtin_driver",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "A CRASHING gate is an OPEN gate that ALSO traps the session - the exact incident this task fixes (lesson hooks-must-fail-closed-with-recovery). Two crash modes: unparseable run.json/state (JSONDecodeError at load) AND unparseable factory_lib.py (SyntaxError at IMPORT, before any code runs). The fallback MUST be SELF-CONTAINED - wrap the factory_lib/forge_cli imports in try/except and, on ImportError/SyntaxError OR a state parse error, run a minimal static deny-list that does NOT import factory_lib (inline the stdin parse + path checks). The deny-list FAILS CLOSED (denies product/canon writes by a hardcoded path-prefix list) but PERMITS git-native recovery (git checkout --ours/--theirs, git rm, git add, git merge/rebase/cherry-pick --abort, git reset) AND read-only tools, so the session self-heals in place. Mid-merge carve-out: when git ls-files -u shows unmerged entries, permit git-native resolution for EXACTLY those paths; Edit/Write content hand-writes to them stay refused; commands on merged paths refused. stop_continue MUST exit 0 (never exit-2) on unparseable state to avoid the re-invoke loop. forge next (phase.py) detects a fresh merge (MERGE_HEAD or a merge commit at HEAD) and runs roadmap heal (done-wins union) once, automatically. The NORMAL parseable-state hook path must not change behavior (existing hook tests stay green). SCOPE: the two hooks + forge next only. ALSO fix the stale existing test test_jsonl_ledgers_merge_with_a_builtin_driver (a task-3 followup, in scope via test_gates.py): task 3 narrowed .gitattributes from `.factory/*.jsonl merge=union` to `.factory/signals.jsonl merge=union` (events.jsonl is frozen/per-file), so the test's pattern tuple must change `.factory/*.jsonl` -> `.factory/signals.jsonl`. Do NOT re-add `.factory/*.jsonl` to .gitattributes (out of scope and wrong).",
      "plan_contracts": [
        {
          "id": "CFS5-C1",
          "statement": "the carve-out matrix passes: resolution allowed for exactly the unmerged set, refused outside it, deny-list fallback engages on broken imports",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS5-C2",
          "statement": "forge next heals the roadmap after a merge without being asked",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    },
    {
      "id": "migration-and-vendor",
      "title": "Legacy repos cut over at intake; clients get it via upgrade",
      "objective": "Activate the story-scoped layout atomically (0046): intake creates .factory/stories/<KEY>/ (the marker) so new stories run new-layout; the audited legacy-path consumers (plans.py, check_board_complete, findings, audit) resolve story-dir-first with legacy fallback so shipped new-layout stories stay visible. Old artifacts untouched and readable; forge upgrade re-vendors to clients. A merge-simulation gate proves two concurrent stories produce zero overlapping .factory paths.",
      "acceptance_criteria": [
        "intake on a legacy fixture starts the new layout with old artifacts untouched and readable",
        "the merge-simulation test merges two concurrent story branches with zero .factory conflicts"
      ],
      "dependencies": [
        "merge-survivable-hooks"
      ],
      "write_scope": [
        "factory/scripts/intake.py",
        "factory/scripts/forge_cli/plans.py",
        "factory/scripts/check_board_complete.py",
        "factory/scripts/forge_cli/findings.py",
        "factory/scripts/forge_cli/audit.py",
        "factory/scripts/forge_cli/phase.py",
        "factory/scripts/pr_ready.py",
        "factory/tests/test_gates.py"
      ],
      "verify_commands": [
        "python3 factory/scripts/check_dual_runtime.py"
      ],
      "required_tests": [
        {
          "id": "test_intake_on_legacy_fixture_starts_new_layout_old_artifacts_readable",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_merge_simulation_two_stories_zero_factory_conflicts",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_shipped_new_layout_story_visible_to_board_and_consumers",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_completed_contract_check_uses_protected_stage_digest",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_decomposition_accepts_empty_required_tests",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_decomposition_accepts_frontier_detail_and_exempts_done_tasks",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_decomposition_provenance_overrides_agent_supplied_fields",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_decomposition_recorder_validates_plan_contracts",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_delegate_readonly_unaffected_by_task_grill",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_delegate_refuses_stale_task_grill",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_delegate_refuses_without_task_grill",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_functional_check_conditional_on_user_facing",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_functional_check_required_when_user_facing",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_intake_after_ship_needs_no_discard",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_intake_preserves_signoff_and_refuses_to_clobber_evidence",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_lite_quality_review_ignores_shipped_plan_contracts",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_next_routes_design_skills_by_feature_type",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_outcome_is_required_to_ship_and_survives_in_the_record",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_plan_grill_recorder_stamps_the_active_issue",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_pr_ready_blocks_on_unverified_plan_contracts",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_pr_ready_legacy_story_still_archives_to_history",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_pr_ready_rejects_unstamped_evidence",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_precompact_scratchpad_snapshots_facts_and_findings",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_project_name_survives_the_run_state_lifecycle",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_quality_review_requires_contract_verdicts",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_recorder_stdin_reads_non_ascii_utf8",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_roadmap_lifecycle",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_ship_archives_the_plan_grill_not_the_project_grills",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_signal_events_block_ship_until_resolved",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_stage_loop_orders_execution_and_gates_pr_ready",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_stage_migrate_records_the_base_on_adopted_stages",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_stage_migrate_refuses_partial_protected_authority[decomposition.json]",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_stage_migrate_requires_confirmation_and_adopts_legacy_state",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_story_timeline_is_recorded_and_archived_with_its_story",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_workspace_decomposition_mirror_cannot_forge_task_contract",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_roadmap_dependency_and_lifecycle_guards",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_stage_loop_orders_execution_and_gates_pr_ready",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_task_digest_arg_is_removed_and_gates_rederive",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest --with psutil python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "This is the ATOMIC activation (0046): intake sets the marker AND every audited legacy-path consumer migrates in the SAME task, gated by a green full suite - no half-migrated state ships. The marker is intake creating .factory/stories/<KEY>/ (story_uses_scoped_layout keys on its existence). Migrate the audited hand-joined reads to story-dir-first + legacy fallback (task 1's discipline): plans.py:135 plan grill + plans.py:78 history stages; check_board_complete.py:31 history dir; findings.py + audit.py history reads - so a shipped new-layout story (evidence in .factory/stories/<KEY>/, no history archive) is visible. AC1: intake on a LEGACY fixture leaves old singletons/history UNTOUCHED and readable (no data migration - legacy stays legacy, new stories go new-layout). AC2 headline: the merge-simulation test intakes two stories in two worktrees, records evidence in each, and merges one branch into the other's base asserting ZERO .factory conflicts. forge upgrade re-vendors scripts wholesale so clients get the change automatically at their next intake - verify the re-vendor includes the new files; no bespoke migration of client data. If the diff overruns the budget, the split is a SIGNAL (consumer-migration vs intake-activation), but they must land in the same story before ship. S-0007: TWO real production regressions the activation exposed (fix behavior, not tests): phase.py hand-reads legacy .factory/{decomposition,tests,verify,reviews} -> route through evidence_path (story-dir-first, legacy fallback) so scoped-story phase + design-skill routing work (test_next_routes_design_skills_by_feature_type); pr_ready's scoped new-layout ship path must handle .factory/scratchpad.md like the legacy path (test_precompact_scratchpad_snapshots_facts_and_findings). phase.py and pr_ready.py added to write_scope. The other 30 failing required tests are stale-test migrations (fix the tests per the two patterns).",
      "plan_contracts": [
        {
          "id": "CFS6-C1",
          "statement": "intake on a legacy fixture starts the new layout with old artifacts untouched and readable",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        },
        {
          "id": "CFS6-C2",
          "statement": "the merge-simulation test merges two concurrent story branches with zero .factory conflicts",
          "source": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md#acceptance-criteria"
        }
      ]
    }
  ],
  "epic": "accountable-engineering",
  "plan_file": "plans/active/FORGE-CFS-1-story-state-stops-conflicting-across-prs.md",
  "plan_sha256": "ba7f3d643e7332cdeb8efe17a1e82d1fd32dbe9b7719190cafc81578c2fef03b",
  "commit": "0c6679987724bcf31ad1886e0b68af47158805fd"
}
