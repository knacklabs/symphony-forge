{
  "generated_by": "claude-code:plan-mode",
  "user_facing": false,
  "story": "FORGE-WIN-1",
  "epic": "windows-zero-setup",
  "plan_file": "plans/active/FORGE-WIN-1-zero-setup-windows-support.md",
  "doc_roots": [
    "docs/specs/windows-zero-setup.md"
  ],
  "tasks": [
    {
      "id": "FORGE-WIN-1.1",
      "title": "Interpreter resolution point + forge.cmd + doctor hook-health + windows CI",
      "objective": "The fail-open hole closes: every registered hook resolves its interpreter through one shared point that fails CLOSED within a working shell, cmd/PowerShell users can run forge natively, doctor executes hooks and reds what cannot run, and a windows-latest job proves the armed path instead of assuming it.",
      "dependencies": [],
      "write_scope": [
        ".claude/settings.json",
        ".codex/hooks.json",
        "forge",
        "forge.cmd",
        "factory/scripts/forge.py",
        "factory/scripts/forge_cli/doctor.py",
        "factory/scripts/forge_cli/phase.py",
        "factory/scripts/forge_cli/scaffold.py",
        ".github/workflows/factory-scaffold.yml",
        "factory/tests/test_gates.py"
      ],
      "acceptance_criteria": [
        "Hook commands in BOTH runtime configs route through one resolution point (the plan proposes a forge hook <name> passthrough with the entrypoint owning the py -3 -> python3 -> python chain; the worker owns the exact form) that is byte-identical in behavior on POSIX and exits 2 (blocking) when no interpreter resolves - never 127-and-vanish. check_dual_runtime stays green.",
        "forge.cmd (new, joins COPY_FILES) runs forge from cmd/PowerShell with zero setup: Git Bash sh when present, else py -3 / python directly against factory/scripts/forge.py.",
        "doctor gains hook-health: loads both runtime configs, executes each registered command with a benign no-op JSON payload, asserts exit 0/2 semantics, reports a named RED row per failing hook; --fast stays existence-only; phase.py names doctor first on a hook-broken clone.",
        "factory-scaffold.yml gains a windows-latest harness-only job running the lockout/degraded/companion gate selectors plus doctor hook-health; POSIX-only tests carry enumerated skip markers.",
        "Full suite green on POSIX; live: after the config change, the session lockout still denies a product write and a recorder still works."
      ],
      "verify_commands": [
        "uv run --with pytest python -m pytest factory/tests/test_gates.py -q -k \"hook_health or forge_cmd or lockout or companion\""
      ],
      "required_tests": [
        {
          "id": "test_doctor_hook_health_reds_unresolvable_hook",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        },
        {
          "id": "test_hook_resolution_fails_closed_without_interpreter",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "The hook-config edit is live in this repo the moment it lands - POSIX behavior must be byte-identical and the suite green before anything else; fail-closed means exit 2 from the resolution point, not from luck; the windows job must not weaken ubuntu coverage; forge.cmd must not fork the interpreter-resolution logic (one point, two entrances)."
    }
  ],
  "project": "",
  "plan_sha256": "a1bcb5c8e9b0fde94fb071a5f7c40a5dd0e24a4020ca5931e1dfc4dcd9d7fc5c",
  "commit": "ea109783647aa37650c1aca988190b409c713635"
}
