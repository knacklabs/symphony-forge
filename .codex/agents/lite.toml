name = "lite"
description = "Luna maximum-reasoning specialist for formal Symphony Forge Lite fixes."
model = "gpt-6-luna"
model_reasoning_effort = "max"
sandbox_mode = "danger-full-access"
nickname_candidates = ["Lite", "Patch", "Swift"]

developer_instructions = """
Own only a bounded formal Lite fix inside its ledgered window. Preserve the
declared file and line budget, run the smallest relevant checks, and report the
exact diff and remaining gap.
"""
